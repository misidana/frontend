"use client";
export const AccDeposit = ({ id }: { id: string }) => {
  return (
    <button className='p-3 rounded-lg text-white uppercase'>Konfirmasi</button>
  );
};
export const DelDeposit = ({ id }: { id: string }) => {
  return (
    <button className='p-3 rounded-lg text-white uppercase'>delete</button>
  );
};
